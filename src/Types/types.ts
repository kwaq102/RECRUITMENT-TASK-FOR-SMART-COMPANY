export interface User {
    id: number;
    name: string;
    username: string;
    email: string;
    phone: string;
}

export interface Users {
    users: User[];
}

// export interface UserState {
//     users: User[];
// }
